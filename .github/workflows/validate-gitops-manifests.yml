name: Validate GitOps manifests

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  validate:
    name: Validate YAML, Kustomize, and Kubernetes manifests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout GitOps repo
        uses: actions/checkout@v4

      - name: Install tools
        run: |
          set -e

          echo "Installing kubeconform..."
          KUBECONFORM_VERSION="0.6.7"
          curl -sSL \
            "https://github.com/yannh/kubeconform/releases/download/v${KUBECONFORM_VERSION}/kubeconform-linux-amd64.tar.gz" \
            -o /tmp/kubeconform.tar.gz

          tar -xzf /tmp/kubeconform.tar.gz -C /tmp
          sudo mv /tmp/kubeconform /usr/local/bin/kubeconform
          kubeconform -v

      - name: Validate YAML syntax
        run: |
          set -e

          echo "Checking YAML files..."
          python3 - <<'PY'
from pathlib import Path
import sys
import yaml

failed = False

for path in sorted(Path(".").rglob("*.yaml")) + sorted(Path(".").rglob("*.yml")):
    if ".git" in path.parts:
        continue

    try:
        with path.open() as f:
            list(yaml.safe_load_all(f))
        print(f"OK: {path}")
    except Exception as e:
        print(f"ERROR: {path}: {e}")
        failed = True

if failed:
    sys.exit(1)
PY

      - name: Render capstone-store base with Kustomize
        run: |
          set -e

          echo "Rendering apps/capstone-store/base..."
          kubectl kustomize apps/capstone-store/base > /tmp/capstone-store-base-rendered.yaml

          echo "Rendered manifest preview:"
          head -80 /tmp/capstone-store-base-rendered.yaml

      - name: Validate rendered manifests with kubeconform
        run: |
          set -e

          kubeconform \
            -strict \
            -summary \
            -ignore-missing-schemas \
            /tmp/capstone-store-base-rendered.yaml
