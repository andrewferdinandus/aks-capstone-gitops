resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: aiops-dashboard-content
    files:
      - index.html
      - incident-report.json
      - nginx.conf

generatorOptions:
  disableNameSuffixHash: true
